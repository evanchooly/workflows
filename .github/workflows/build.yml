name: Build and Test

on:
  workflow_call:
    inputs:
      archive-name:
        required: false
        type: string
        default: "${{ github.sha }}-${{ github.run_id }}"
      java:
        required: false
        type: string
        default: 11
      maven-flags:
        required: false
        type: string
        default: ''
      optional:
        required: false
        type: boolean
        default: false
      os:
        required: false
        type: string
        default: ubuntu-latest
      reuseBuild:
        required: false
        type: boolean
        default: false
      saveBuild:
        required: false
        type: boolean
        default: false
    secrets:
      GH_TOKEN:
        required: false

env:
  GITHUB_OAUTH: ${{ secrets.GH_TOKEN }}

jobs:
  Build:
    runs-on: ${{ inputs.os }}
    continue-on-error: ${{ inputs.optional }}
    name: Build
    outputs:
      maven-cache-key: ${{ steps.maven-cache-key.outputs.key }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Generate cache key
        id: maven-cache-key
        run: |
          echo "key=maven-cache-$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
        shell: bash

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: ${{ inputs.java }}
          distribution: 'temurin'
          server-id: central
          cache: maven

      - name: Find maven
        id: maven
        shell: bash
        run: echo "maven=$( test -f mvnw  && echo "./mvnw" || echo mvn )" >> $GITHUB_OUTPUT

      - name: Build with Tests
        if: ${{ !inputs.saveBuild && !inputs.reuseBuild }}
        run: ${{ steps.maven.outputs.maven }} -U install ${{ inputs.maven-flags }}

      - name: Build without Tests
        if: inputs.saveBuild
        run: ${{ steps.maven.outputs.maven }} -U install -DskipTests ${{ inputs.maven-flags }}

      - name: Cache build artifacts
        if: inputs.saveBuild
        uses: actions/cache@v3
        with:
          path: '**/target'
          key: ${{ steps.maven-cache-key.outputs.key }}
          restore-keys: ${{ steps.maven-cache-key.outputs.key }}

      - name: Restore cached build artifacts
        if: inputs.reuseBuild
        uses: actions/cache@v3
        with:
          path: '**/target'
          key: ${{ steps.maven-cache-key.outputs.key }}
          restore-keys: ${{ steps.maven-cache-key.outputs.key }}
          fail-on-cache-miss: true

      - name: Test without building
        if: inputs.reuseBuild
        run: ${{ steps.maven.outputs.maven }} surefire:test ${{ inputs.maven-flags }}

      - name: collect logs
        uses: actions/upload-artifact@v3
#        if: failure() && inputs.archive-name != ''
        with:
          name: "logs-${{ inputs.archive-name }}"
          path: |
            **/target
            **/target/**/*.log
            **/target/**/*.err
          retention-days: 7
